import Phaser from 'phaser';

// Overlay de diálogo (06_INTERFACE_UX.md, Seção 4).
// TODO: caixa de diálogo, retratos, efeito typewriter, avançar/acelerar texto.
export default class DialogueOverlay extends Phaser.Scene {
  constructor() {
    super('DialogueOverlay');
  }

  create() {
    // Placeholder — lançada via scene.launch() ao interagir com NPC.
  }
}
