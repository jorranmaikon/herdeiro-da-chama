import Phaser from 'phaser';

// Tela dedicada de Crônicas (06_INTERFACE_UX.md, Seção 5).
// TODO: imagem ilustrada, efeito Ken Burns, fade, texto narrado sincronizado.
export default class ChronicleScene extends Phaser.Scene {
  constructor() {
    super('ChronicleScene');
  }

  create() {
    // Placeholder — substitui a HUD normal por completo quando ativa.
  }
}
